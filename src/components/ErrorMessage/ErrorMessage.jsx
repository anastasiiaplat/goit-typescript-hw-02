

const ErrorMessage = () => {
  return (
    <div style={{ textAlign: 'center', marginTop: '20px', color: 'red' }}>
      <p>Try updating the page or try again later.</p>
    </div>
  );
};

export default ErrorMessage;
